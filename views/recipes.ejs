<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipes</title>

    <link rel="stylesheet" href="/css/layout.css" />
    <link rel="stylesheet" href="/css/recipes.css">
    <link rel="stylesheet" href="https://unicons.iconscout.com/release/v4.0.0/css/line.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <script src="/scripts/delete-recipe.js" defer></script>
    <script src="/scripts/recipe-modal.js" defer></script>
    <script src="/scripts/recipes.js" defer></script>
    <script src="/scripts/filter.js" defer>
    </script>
</head>
<body>
<div class="app-layout">

    <%- include('partials/navbar.ejs') %>

    <main class="page-content">
        <div class="recipe-header">
            <h1>Your Recipes</h1>

            <div class="page-actions">
                <!-- Filter Bar, (Used for the button style) -->
                <div class= "FilterBarContainer">
                    <button class="FilterBtn" id= "OpenFilter"> 
                    <i class="fa-solid fa-filter"> </i> 
                    Filter
                    </button>  
                </div>

                <!-- Recipe Creation UI -->
                <button class="addRecipeBtn" id="OpenRecipeCreation" onclick="OpenRecipeCreationUI()">+ Add Recipe</button>
            </div>
        </div>

        <!-- Search Bar UI -->
        <form action="/recipes" method="GET" class="SearchWrapper">
            <!-- If filters are applied save them -->
            <input type="hidden" name="time" value="<%= time %>">
            <input type="hidden" name="cost" value="<%= cost %>">
            <input type="hidden" name="difficulty" value="<%= difficulty %>">
            <input type="hidden" name="dietary" value="<%= dietary %>">

            <i class="uil uil-search"></i> 
            <input type="text" name="search" id="SearchInput" placeholder="Search recipes..." class="SearchInput" value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>">
            <button type="submit" class="SearchButton">Search</button> 
        </form>

        <!-- Overlay for the Filter menu-->
        <div id="FilterOverlay" class="FilterOverlay">
            <div class="OverlayContent">
                <span class="CloseBtn">&times;</span>
                <h2 class="FilterTitle">Filter Recipes</h2>
                <form class = "FilterForm" action="/recipes" method="GET">
                    <!-- if there are search query save them -->
                    <input type="hidden" name="search" value="<%= searchQuery %>">

                    <select class="FilterSelect" name="time" >
                        <option class="OptionStyle" value="none" <%= time === 'none' ? 'selected' : '' %>>Any Time</option>
                        <option class="OptionStyle" value="15" <%= time === '15' ? 'selected' : '' %>>Under 15 mins</option>
                        <option class="OptionStyle" value="30" <%= time === '30' ? 'selected' : '' %>>Under 30 mins</option>
                        <option class="OptionStyle" value="60" <%= time === '60' ? 'selected' : '' %>>Under 1 hour</option>
                    </select>
                    <select class="FilterSelect" name="cost" >
                        <option class="OptionStyle" value="none" <%= cost === 'none' ? 'selected' : '' %>>Any Cost</option>
                        <option class="OptionStyle" value="low" <%= cost === 'low' ? 'selected' : '' %>>Low Cost</option>
                        <option class="OptionStyle" value="medium" <%= cost === 'medium' ? 'selected' : '' %>>Medium Cost</option>
                        <option class="OptionStyle" value="high" <%= cost === 'high' ? 'selected' : '' %>>High Cost</option>
                    </select>
                    
                    <select class="FilterSelect" name="difficulty" >
                        <option class="OptionStyle" value="none" <%= difficulty === 'none' ? 'selected' : '' %>>Any Difficulty</option>
                        <option class="OptionStyle" value="easy" <%= difficulty === 'easy' ? 'selected' : '' %>>Easy</option>
                        <option class="OptionStyle" value="medium" <%= difficulty === 'medium' ? 'selected' : '' %>>Medium</option>
                        <option class="OptionStyle" value="hard" <%= difficulty === 'hard' ? 'selected' : '' %>> Hard</option>
                    </select>
                   
                    <select class="FilterSelect" name="dietary" >
                        <option class="OptionStyle" value="none" <%= dietary === 'none' ? 'selected' : '' %>>Any Dietery</option>
                        <option class="OptionStyle" value="vegan" <%= dietary === 'vegan' ? 'selected' : '' %>>Vegan</option>
                        <option class="OptionStyle" value="vegetarian" <%= dietary === 'vegetarian' ? 'selected' : '' %>>Vegetarian</option>
                        <option class="OptionStyle" value="gluten-free" <%= dietary === 'gluten-free' ? 'selected' : '' %>>Gluten-free</option>
                    </select>
                    <input type="hidden" class="filteringOn" name="filterOn" value="true">
                    <button type="submit" onclick="" class="FilterBtn">Apply Filters</button>
                    
                </form>
            </div>
        </div>
        <!-- End of Filter Overlay -->

        <!-- Recipe creation UI -->
        <div class = "RecipeCreationUI" id="RecipeCreationUIPopUp">
            <div class = "RecipeCreationOverlay">
                    <span class="CloseBtnRecipeCreation">&times;</span>
                    <h2 class="RecipeCreationTitle">Create a New Recipe</h2>
                    
                    <form id="RecipeForm" class = "RecipeCreationForm" action="/recipes" method="POST">
                        <div class = "Step active">
                            <label for="name" class="StepLabel">Recipe Name</label >
                            <input type="text" id="RecipeName" name="name" placeholder="Enter the name of your recipe" required class="StepInputRecipe">
                            <div class="ErrorMessage" id="RecipeNameError"></div>
                            <div class="StepButtons">
                            <button type="button" onclick="NextStep()" class="NextBackButtonStyle">Next</button>
                            </div>
                        </div>
                        <div class = "Step">
                            <label class="StepLabel">Add ingredients</label>
                            <div>
                                <input type="text" id="IngredientInput" placeholder="Enter ingredient" class="StepInputRecipe">
                                <button type="button" onclick="addIngredient()" class="AddIngredientButton">Add Ingredient</button>
                            </div>
                            <ul id="IngredientList"></ul>
                            <input type="hidden" name="ingredients" id="IngredientsHidden">
                            <div class="ErrorMessage" id="ingredientError"></div>
                            <div class="StepButtons">
                                <button type="button" onclick="PrevStep()" class="NextBackButtonStyle">Back</button>
                                <button type="button" onclick="NextStep()" class="NextBackButtonStyle">Next</button>
                            </div>
                        </div> 
                        <div class="Step">
                            <label class="StepLabel">Enter preparation steps</label>
                            <textarea name="Steps" required class="StepInputRecipe"></textarea>
                            <div class="ErrorMessage" id="stepError"></div>
                            <div class="StepButtons">
                                <button type="button" onclick="PrevStep()" class="NextBackButtonStyle">Back</button>
                                <button type="button" onclick="NextStep()" class="NextBackButtonStyle">Next</button>
                            </div>
                        </div>
                        <div class="Step">
                            <label class="StepLabel">Preparation time</label>
                            <input type="text" name="time" placeholder="Enter the preparation time" id="Prep" class="StepInputRecipe" required>
                        <div class="ErrorMessage" id="PrepError">   
                        </div>
                            <div class="StepButtons">
                                <button type="button" onclick="PrevStep()" class="NextBackButtonStyle">Back</button>
                                <button type="button" onclick="NextStep()" class="NextBackButtonStyle">Next</button>
                            </div>
                        </div>
                        <div class="Step">
                            <label class="StepLabel">Cost</label>
                            <input type="text" name="cost" placeholder="Enter the Cost" id="Cost" class="StepInputRecipe" required>
                            <div class="ErrorMessage" id="CostError"></div>
                            <div class="StepButtons">
                                <button type="button" onclick="PrevStep()" class="NextBackButtonStyle">Back</button>
                                <button type="button" onclick="NextStep()" class="NextBackButtonStyle">Next</button>

                            </div>
                        </div>
                        <div class="Step">
                            <label class="StepLabel">Dietary tag</label>
                            <select name="tags" id="Dietary" class="StepInputRecipe" required>
                                <option value="">Select dietary tag</option>
                                <option value="None">None</option>
                                <option value="Vegan">Vegan</option>
                                <option value="Keto">Keto</option>
                                <option value="Vegetarian">Vegetarian</option>
                                <option value="Protein+">Protein+</option>
                                <option value="Spicy">Spicy</option>
                                <option value="Liquid">Liquid</option>
                                <option value="Heart-health">Heart health</option>
                                <option value="Halal">Halal</option>
                                <option value="Lactose-free">Lactose-free</option>
                            </select>
                            <div class="ErrorMessage" id="DietaryError"></div>
                            <div class="StepButtons">
                                <button type="button" onclick="PrevStep()" class="NextBackButtonStyle">Back</button>
                                <button type="button" onclick="NextStep()" class="NextBackButtonStyle">Next</button>
                            </div>
                        </div>
                        <div class="Step">
                            <label class="StepLabel">Difficulty</label>
                            <select name="difficulty" id="difficulty" class="StepInputRecipe" required>
                                <option value="">Select difficulty</option>
                                <option value="easy">Easy</option>
                                <option value="medium">Medium</option>
                                <option value="hard">Hard</option>
                            </select>
                            <div class="ErrorMessage" id="DifficultyError"></div>
                            <div class="StepButtons">
                                <button type="button" onclick="PrevStep()" class="NextBackButtonStyle">Back</button>
                                <button type="submit" class="SubmitRecipeButtonStyle">Submit Recipe</button>
                            </div>
                        </div>
                    </form>
            </div>
        </div>
        <!-- End of Recipe Creation UI -->

        <!-- Edit Recipe UI -->
        <div class="RecipeCreationUI" id="RecipeEditUIPopUp">
            <div class="RecipeCreationOverlay">
                <span class="CloseBtnRecipeEdit">&times;</span>
                <h2 class="RecipeCreationTitle">Edit Recipe</h2>
                <form id="EditRecipeForm" class="RecipeCreationForm" action="" method="POST">
                    <input type="hidden" name="_method" value="PUT">
                    <div class="Step active">
                        <label for="EditRecipeName" class="StepLabel">Recipe Name</label>
                        <input type="text" id="EditRecipeName" name="name" placeholder="Enter the name of your recipe" required class="StepInputRecipe">
                        <div class="ErrorMessage" id="EditRecipeNameError"></div>
                        <div class="StepButtons">
                            <button type="button" onclick="EditNextStep()" class="NextBackButtonStyle">Next</button>
                        </div>
                    </div>
                    <div class="Step">
                        <label class="StepLabel">Add ingredients</label>
                        <div>
                            <input type="text" id="EditIngredientInput" placeholder="Enter ingredient" class="StepInputRecipe">
                            <button type="button" onclick="addEditIngredient()" class="AddIngredientButton">Add Ingredient</button>
                        </div>
                        <ul id="EditIngredientList"></ul>
                        <input type="hidden" name="ingredients" id="EditIngredientsHidden">
                        <div class="ErrorMessage" id="EditIngredientError"></div>
                        <div class="StepButtons">
                            <button type="button" onclick="EditPrevStep()" class="NextBackButtonStyle">Back</button>
                            <button type="button" onclick="EditNextStep()" class="NextBackButtonStyle">Next</button>
                        </div>
                    </div>
                    <div class="Step">
                        <label class="StepLabel">Enter preparation steps</label>
                        <textarea name="Steps" id="EditSteps" required class="StepInputRecipe"></textarea>
                        <div class="ErrorMessage" id="EditStepError"></div>
                        <div class="StepButtons">
                            <button type="button" onclick="EditPrevStep()" class="NextBackButtonStyle">Back</button>
                            <button type="button" onclick="EditNextStep()" class="NextBackButtonStyle">Next</button>
                        </div>
                    </div>
                    <div class="Step">
                        <label class="StepLabel">Preparation time</label>
                        <input type="text" name="time" placeholder="Enter the preparation time" id="EditPrep" class="StepInputRecipe" required>
                        <div class="ErrorMessage" id="EditPrepError"></div>
                        <div class="StepButtons">
                            <button type="button" onclick="EditPrevStep()" class="NextBackButtonStyle">Back</button>
                            <button type="button" onclick="EditNextStep()" class="NextBackButtonStyle">Next</button>
                        </div>
                    </div>
                    <div class="Step">
                        <label class="StepLabel">Cost</label>
                        <input type="text" name="cost" placeholder="Enter the Cost" id="EditCost" class="StepInputRecipe" required>
                        <div class="ErrorMessage" id="EditCostError"></div>
                        <div class="StepButtons">
                            <button type="button" onclick="EditPrevStep()" class="NextBackButtonStyle">Back</button>
                            <button type="button" onclick="EditNextStep()" class="NextBackButtonStyle">Next</button>
                        </div>
                    </div>
                    <div class="Step">
                        <label class="StepLabel">Dietary tag</label>
                        <select name="tags" id="EditDietary" class="StepInputRecipe" required>
                                <option value="">Select dietary tag</option>
                                <option value="None">None</option>
                                <option value="Vegan">Vegan</option>
                                <option value="Keto">Keto</option>
                                <option value="Vegetarian">Vegetarian</option>
                                <option value="Protein+">Protein+</option>
                                <option value="Spicy">Spicy</option>
                                <option value="Liquid">Liquid</option>
                                <option value="Heart-health">Heart health</option>
                                <option value="Halal">Halal</option>
                                <option value="Lactose-free">Lactose-free</option>
                        </select>
                        <div class="ErrorMessage" id="EditDietaryError"></div>
                        <div class="StepButtons">
                            <button type="button" onclick="EditPrevStep()" class="NextBackButtonStyle">Back</button>
                            <button type="button" onclick="EditNextStep()" class="NextBackButtonStyle">Next</button>
                        </div>
                    </div>
                    <div class="Step">
                        <label class="StepLabel">Difficulty</label>
                        <select name="difficulty" id="EditDifficulty" class="StepInputRecipe" required>
                            <option value="">Select difficulty</option>
                            <option value="easy">Easy</option>
                            <option value="medium">Medium</option>
                            <option value="hard">Hard</option>
                        </select>
                        <div class="ErrorMessage" id="EditDifficultyError"></div>
                        <div class="StepButtons">
                            <button type="button" onclick="EditPrevStep()" class="NextBackButtonStyle">Back</button>
                            <button type="submit" class="SubmitRecipeButtonStyle">Update Recipe</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
        <!-- End of Edit Recipe UI -->

        <% if (flashMessage) { %>
            <div class="flash-message">
                <%= flashMessage %>
            </div>
        <% } %>

        <% if (flashError) { %>
            <div class="flash-error">
                <%= flashError %>
            </div>
        <% } %>

        <section class="recipe-list">
            <% if (recipes.length > 0) { %>
                <% recipes.forEach(recipe => { %>

                    <div class="recipe-card"
                        data-id="<%= recipe.id %>"
                        data-name="<%= recipe.name %>"
                        data-ingredients='<%- JSON.stringify(recipe.ingredients) %>'
                        data-preptime="<%= recipe.prepTime %>"
                        data-steps="<%= recipe.prepSteps %>"
                        data-cost="<%= recipe.cost %>"
                        data-tag="<%= recipe.tag %>"
                        data-difficulty="<%= recipe.difficulty %>">

                        <div class="card-tags">
                            <% if (recipe.tag) { %>
                                <span class="recipe-tag"><%= recipe.tag %></span>
                            <% } %>

                            <% if (recipe.difficulty) { %>
                                <span class="recipe-tag <%= recipe.difficulty %>">
                                    <%= recipe.difficulty %>
                                </span>
                            <% } %>
                        </div>

                        <!-- Edit/Delete icons -->
                        <div class="card-top-actions">
                            <button type="button" class="icon-btn edit-icon edit-recipe-btn" data-recipe-id="<%= recipe.id %>">
                                <i class="fa-solid fa-pen"></i>
                            </button>

                            <form action="/recipes/<%= recipe.id %>?_method=DELETE" method="POST">
                                <button type="submit" class="icon-btn delete-icon deleteBtn">
                                    <i class="fa-solid fa-trash"></i>
                                </button>
                            </form>
                        </div>

                        <h3><%= recipe.name %></h3>

                        <!-- Prep time + Cost -->
                        <div class="recipe-meta">
                            <span><i class="fa-solid fa-clock"></i> <%= recipe.prepTime %></span>
                            <span><%= recipe.cost %></span>
                        </div>

                    </div>

                <% }); %>
            <% } else { %>
        <div class="empty-state">
            <% if (typeof searchQuery !== 'undefined' && searchQuery) { %>
                <p class="empty-message">No results found for "<strong><%= searchQuery %></strong>".</p>
                <a href="/recipes" class="NextBackButtonStyle" style="text-decoration: none; display: inline-block;">
                    <i class="fa-solid fa-arrow-left"></i> Return to Recipes
                </a>
            <% } else { %>
                <p class="empty-message">No recipes yet. Add a recipe to see it here.</p> [cite: 44]
                <% } %>
            </div>
            <% } %>
        </section>

        <!-- MODAL -->
        <div id="recipeModal" class="modal hidden">
            <div class="modal-content">
                <span id="closeModal" class="close-btn">&times;</span>

                <h2 id="modalName"></h2>
                
                <div class="modal-tags">
                    <span id="modalTag" class="recipe-tag modal-tag"></span>
                    <span id="modalDifficulty" class="recipe-tag modal-tag"></span>
                </div>

                <p><strong>Ingredients:</strong></p>
                <ul id="modalIngredients"></ul>

                <p><strong>Prep Time:</strong> <span id="modalPrepTime"></span></p>

                <p><strong>Steps:</strong></p>
                <p id="modalSteps"></p>

                <p><strong>Cost:</strong> <span id="modalCost"></span></p>
            </div>
        </div>

    </main>
  </div>
</body>
</html>